{
  "version": 2,
  "buildCommand": "pnpm install && pnpm add framer-motion && pnpm build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  }
}
